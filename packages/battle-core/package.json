{
  "name": "@rangu/battle-core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "description": "랑구 택틱스 FE 상태 코어 — battleStore(Zustand) + MessageMapper (proto 바이너리 → 스토어).",
  "scripts": {
    "test": "vitest run",
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "@rangu/proto-ts": "file:../proto-ts",
    "protobufjs": "^7.4.0",
    "zustand": "^5.0.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
